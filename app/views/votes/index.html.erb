<p id="notice"><%= notice %></p>

<h1>Abstimmungen</h1>

<div class="row">
  <div class="col s12 m6 offset-m3">
    <div class="card amber">
      <div class="card-content">
        <span class="card-title">Hinweis</span>
        <p>Umfrageergebnisse sind unter dem Punkt '<%= link_to 'Umfragen', polls_path %>' einzusehen!</p>
        <p>Abgestimmt wird aber zunächst weiterhin hier.</p>
      </div>
    </div>
  </div>
</div>

<table>
  <thead>
    <tr>
      <th>Umfrage</th>
      <th>Item</th>
      <th>Wähler</th>
      <th>Präferenz</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @votes.each do |vote| %>
      <tr>
        <td><%= get_poll_for_proposal(vote.proposal).name %></td>
        <td><%= get_item_for_proposal(vote.proposal).name %></td>
        <td><%= vote.voter.name %></td>
        <td><%= translate_preference vote.preference %></td>
        <td><%= link_to 'Anzeigen', vote %></td>
        <td><%= link_to 'Editieren', edit_vote_path(vote) %></td>
        <td><%= link_to 'Löschen', vote, method: :delete, data: { confirm: 'Bist du sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Abstimmung', new_vote_path %>
