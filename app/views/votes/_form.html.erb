<%= form_with(model: vote, local: true) do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :proposal_id %>
    <%= form.text_field :proposal_id, id: :vote_proposal_id %>
  </div>

  <div class="field">
    <%= form.label :voter_id %>
    <%= form.text_field :voter_id, id: :vote_voter_id %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :vote_weight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
